FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY src/ ./src/
COPY dashboard/ ./dashboard/
COPY config/ ./config/

ENV EXPIRY_DB_PATH=/app/data/expiry.db

# Default command runs the daily check (ingest + notify).
# Override with a different command to run the dashboard instead:
#   docker run <image> streamlit run dashboard/app.py --server.port=8501 --server.address=0.0.0.0
CMD ["python", "src/run_daily.py"]